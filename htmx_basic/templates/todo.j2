<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Todoリスト</title>
        <meta name="description" content="Todoリスト">
        <meta name="keywords" content="">
        <link rel="icon" href="images/favicon.png" type="image/png">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="css/style.css">
    </head>
    <body class="d-flex justify-content-center">
        <div class="card w-25 shadow p-3 mt-5">
            <h1 class="card-title display-3 text-center text-black-50">Todoリスト</h1>
            <form hx-post="/add" hx-target="#list" hx-swap="beforeend">
                <label class="form-group d-flex">
                    <input name="title" class="form-control">
                    <button class="btn btn-secondary">追加</button>
                </label>
            </form>
            <ul id="list" class="list-group my-3">
                {% for todo in todos %}
                    {% include '_todo.j2' %}
                {% endfor %}
            </ul>
            <a href="{{ url_for('index') }}" class="btn btn-lg text-muted"><i class="fa fa-share"></i>トップへ戻る</a>            
        </div>
        <script src="https://unpkg.com/htmx.org@1.9.10"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
