<aside class="sidebar col-md-3 fade-in fade-delay-1">
    <div class="position-sticky" style="top: 2rem;">
        <div class="mb-3">
            <h4 class="text-bold"><i class="fa fa-heart"></i> いいねランキングTOP10</h4>
            <p class="mb-0">皆さんが「いいね」を押した数の多い順上位10個の記事を紹介しています。</p>
        </div>
        <ul class="list-unstyled pb-5">
            {% for row in top10 %}
                <li class="fade-in fade-delay-{{ loop.index }}">
                    <a class="d-flex flex-md-row gap-3 align-items-start align-items-center py-3 link-body-emphasis text-decoration-none border-top"
                        href="{{ url_for('public.detail', memo_id=row.memo.id) }}">
                        <img class="rounded change-mode"
                            src="{{ url_for('static', filename='images/memo/' ~ row.memo.image_filename) }}"
                            width="96"
                            height="96"
                            alt="記事画像"
                            style="object-fit: cover">
                        <div class="col-8">
                            <div class="category_tag mb-2">
                                {% for category in row.memo.categories %}
                                    <span class="badge rounded-pill" style="background-color: {{ category.color }}">{{ category.name }}</span>
                                {% endfor %}
                            </div>
                            <h6 class="mb-0" style="line-height:1.6">{{ row.memo.title }}</h6>
                            <small class="text-body-secondary"><i class="fa fa-calendar-o"></i> {{ row.memo.created_at.strftime("%Y年%m月%d日") }}</small>
                            <span class="text-body-secondary" style="font-size:0.8rem"><i class="fa fa-gratipay" style="color:#603"></i>{{ row.like_count }}いいね</span>
                        </div>
                    </a>
                </li>
            {% endfor %}
            <li class="mt-3"><a href="https://www.youtube.com/" target="_blank" class="fade-in fade-delay-16 ban-operation"><img class="w-100" src="{{ url_for('static', filename='images/other/banner.jpg') }}" alt="機能紹介バナー" width="" height=""></a></li>
        </ul>
    </div>
</aside>
