{% extends "admin/base.j2" %}
{% block page_title %}ユーザーサムネイル管理 | {{ SITE_NAME }}{% endblock %}
{% block content %}
    <div class="py-4">
        <h2 class="h4 mb-4 fade-in fade-in-delay-1"><i class="fa fa-picture-o me-2"></i>ユーザーサムネイル管理</h2>
        {# PC用テーブル #}
        <div class="table-responsive fade-in fade-in-delay-2 d-none d-md-block">
            <table class="table table-striped table-hover table-sm align-middle">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>サムネイル</th>
                        <th>ユーザー名</th>
                        <th>メール</th>
                        <th>ファイル名</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in users %}
                    <tr>
                        <td class="text-body-secondary small">{{ user.id }}</td>
                        <td>
                            <img src="{{ url_for('static', filename='images/user/' ~ user.thumbnail) }}"
                                 alt="{{ user.username }}"
                                 class="rounded-circle"
                                 width="40" height="40"
                                 style="object-fit:cover;">
                        </td>
                        <td class="fw-bold">{{ user.username }}<span class="ms-1 text-body-secondary small fw-normal">様</span></td>
                        <td class="text-body-secondary small">{{ user.email }}</td>
                        <td class="text-body-secondary small"><code>{{ user.thumbnail }}</code></td>
                        <td>
                            <button type="button" class="btn btn-outline-secondary btn-sm" disabled>
                                <i class="fa fa-upload me-1"></i>変更
                            </button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        {# スマホ用カードリスト #}
        <div class="d-md-none fade-in fade-in-delay-2">
            {% for user in users %}
            <div class="border rounded p-3 mb-3 shadow-sm d-flex align-items-center gap-3">
                <img src="{{ url_for('static', filename='images/user/' ~ user.thumbnail) }}"
                     alt="{{ user.username }}"
                     class="rounded-circle flex-shrink-0"
                     width="52" height="52"
                     style="object-fit:cover;">
                <div class="flex-grow-1 overflow-hidden">
                    <div class="fw-bold">{{ user.username }}<span class="ms-1 text-body-secondary small fw-normal">様</span></div>
                    <div class="text-body-secondary small text-truncate">{{ user.email }}</div>
                    <code class="small text-body-secondary">{{ user.thumbnail }}</code>
                </div>
                <button type="button" class="btn btn-outline-secondary btn-sm flex-shrink-0" disabled>
                    <i class="fa fa-upload"></i>
                </button>
            </div>
            {% endfor %}
        </div>
        <div class="mt-3 fade-in fade-in-delay-3">
            <p class="text-body-secondary small"><i class="fa fa-info-circle me-1"></i>サムネイルの一括管理機能は実装予定です。</p>
            <a class="btn btn-outline-secondary" href="{{ url_for('admin.index') }}"><i class="fa fa-reply me-1"></i>ダッシュボードへ戻る</a>
        </div>
    </div>
{% endblock content %}
